# Copy this file to .env before starting Docker Compose. Do not commit real secrets.
APP_ENV=dev
LOG_LEVEL=INFO

# Public entry point
GATEWAY_HTTP_PORT=8080
VITE_API_BASE_URL=/api/v1
VITE_APP_ENV=dev

# Local infrastructure. Replace every value before deploying outside a local machine.
POSTGRES_USER=ecommerce
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_PORT=5432
REDIS_PORT=6379
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_DEFAULT_USER=ecommerce
RABBITMQ_DEFAULT_PASS=change-me-local-only
CONSUL_PORT=8500

# Sensitive service configuration is injected at runtime; these values are deliberate placeholders.
JWT_PUBLIC_KEY=replace-with-jwt-public-key
INTERNAL_TOKEN=replace-with-internal-service-token

# Optional observability profile ports
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-local-only
JAEGER_UI_PORT=16686
ELASTICSEARCH_PORT=9200
KIBANA_PORT=5601
